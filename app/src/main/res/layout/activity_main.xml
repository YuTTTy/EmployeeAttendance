<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.SwipeRefreshLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/swipe_refresh_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <RelativeLayout
        android:id="@+id/main_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <RelativeLayout
            android:id="@+id/main_title_layout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/titleBg"
            android:padding="@dimen/usually_spacing_big">

            <ImageView
                android:layout_width="@dimen/usually_image_hight"
                android:layout_height="@dimen/usually_image_hight"
                android:src="@drawable/main_title_icon"
                android:contentDescription="@null" />
        </RelativeLayout>

        <FrameLayout
            android:id="@+id/main_body"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_below="@+id/main_title_layout">

        </FrameLayout>

        <android.support.v4.app.FragmentTabHost
            android:id="@android:id/tabhost"
            android:layout_width="match_parent"
            android:layout_height="55dp"
            android:layout_alignParentBottom="true"
            android:background="@color/alpha">

        </android.support.v4.app.FragmentTabHost>


    </RelativeLayout>
</android.support.v4.widget.SwipeRefreshLayout>