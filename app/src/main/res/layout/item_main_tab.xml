<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="60dp"
    android:background="@color/white">

    <RelativeLayout
        android:id="@+id/item_main_tab_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:background="@color/white"
        android:paddingBottom="@dimen/usually_spacing_small"
        android:paddingTop="@dimen/usually_spacing_small">

        <ImageView
            android:id="@+id/item_main_tab_image"
            android:layout_width="30dp"
            android:layout_height="30dp"
            android:layout_centerHorizontal="true"
            android:src="@drawable/ic_launcher_round" />

        <TextView
            android:id="@+id/item_main_tab_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@+id/item_main_tab_image"
            android:layout_centerHorizontal="true"
            android:textColor="@color/baseTextSub"
            android:textSize="@dimen/text_assist_content" />

    </RelativeLayout>

</RelativeLayout>